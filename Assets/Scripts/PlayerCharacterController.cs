// Gameplay and lobby controller for player avatar
// Kept as a prefab-compatible wrapper so the packaged character prefabs
// can continue referencing this script asset without being rebuilt first.
public class PlayerCharacterController : PlayerController
{
}
